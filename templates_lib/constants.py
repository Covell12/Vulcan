"""Constants shared by every template. Small on purpose — only things that
genuinely repeat across templates belong here (see CLAUDE.md: don't build
abstractions for things that don't repeat yet)."""

# PETG default minimum printable wall, per CLAUDE.md.
MIN_WALL_MM = 2.4
